# AegisVault（盾匣）实施方案

> 依据《项目规划.md》制定。本文档为可执行的技术实施方案与分阶段验收清单。
> 状态：已确认（UI 选型、MVP 范围、许可证、AOT 路线）；技术可行性已实测验证，详见《技术可行性验证报告.md》。
> 更新日期：2026-09-12

---

## 0. 调研结论

| 项目 | 结论 |
|---|---|
| 代码现状 | 零代码绿地（仅 `项目规划.md`），无 git、无历史包袱 |
| .NET SDK | 10.0.301（含运行时 10.0.9），可直接 `net10.0` |
| NuGet | 全局缓存位于 `D:\Users\.nuget\packages`；`api.nuget.org` 大文件下载不稳定（EOF/卡死），需配置华为云镜像回退 |
| dotnet-ef | 未安装（本项目不使用 EF Core，无需安装） |
| 关键修正 | 规划文档写 "Avalonia v11.0+"，但 **AtomUI 6.x 要求 Avalonia 12.x**（6.1.9 依赖 Avalonia ≥ 12.1.2）。已确认采用 **Avalonia 12.1.2 + AtomUI 6.1.9** |
| 实现方式调整 | ① Windows 安全输入改用系统 `CredUI` 安全桌面提示（比自建 `CreateDesktop` 可靠）；② 加密 IV/Tag 存 BLOB 而非 Base64（语义不变，开销更小） |
| 实测验证状态 | Avalonia+AtomUI 编译/运行/全量裁剪（0 警告）/托盘、Argon2id、安全内存 P/Invoke、AES-GCM、SQLite 3.53.3、DPAPI、**Windows NativeAOT 端到端（38.7MB 原生单文件）** 均已通过 |

---

## 1. 技术选型（已锁定）

| 用途 | 选型 | 版本策略 | 说明 |
|---|---|---|---|
| 运行时 | .NET 10 | `net10.0` | `PublishAot=true`、`IsAotCompatible=true` |
| UI 框架 | Avalonia | 12.1.2 | `AvaloniaUseCompiledBindingsByDefault=true` |
| 组件库 | AtomUI | 6.1.9+（**LGPL-3.0**） | 主包 `AtomUI.Desktop.Controls`；按需 `AtomUI.Desktop.Controls.DataGrid`、`AtomUI.Core`、`AtomUI.Fonts.AlibabaSans`、`AtomUI.Icons.AntDesign` |
| MVVM | CommunityToolkit.Mvvm | 8.4.2 | 全程源生成器，避免反射绑定 |
| 数据库 | Microsoft.Data.Sqlite | **10.0.12** | 官方包；不用 EF Core（裁剪/AOT 风险） |
| SQLite 原生 | SQLitePCLRaw.bundle_e_sqlite3 | **2.1.12（显式锁定）** | 2.1.11 含 CVE-2025-6965（High）；2.1.12 为 SQLite 3.53.3 |
| KDF | Argon2id（libsodium） | Sodium.Core 1.4.1；AOT 静态链接用 libsodium.nativeaot 1.0.22.1 | 实测 API：`PasswordHash.ArgonHashBinary(..., StrengthArgon, 32, ArgonAlgorithm.Argon_2ID13)`；KDF 算法字段化，支持 PBKDF2-HMAC-SHA256 回退 |
| 数据加密 | `System.Security.Cryptography.AesGcm` | BCL | 硬件加速、AOT 安全；随机 12B nonce + 16B tag + AAD |
| JSON | System.Text.Json 源生成上下文 | BCL | 所有模型走 `JsonSerializerContext` |
| 安全内存 | libsodium `sodium_malloc`/`sodium_memzero` | **自研 `LibraryImport` P/Invoke 封装**（Sodium.Core 无 SecureMemory API，实测） | 密钥不落 GC 堆；`SecureBuffer` 统一释放即清零 |
| 平台密钥保护 | Windows DPAPI（macOS/Linux 接口占位） | `System.Security.Cryptography.ProtectedData` 10.0.12 | 实测 CurrentUser 往返正常；"记住设备"默认关闭 |
| 测试 | xUnit + Avalonia.Headless.XUnit | 12.1.2 | Core 单测 + ViewModel 无头测试 |
| 包管理 | Central Package Management | `Directory.Packages.props` | 统一版本 |
| SDK 固定 | `global.json` | 10.0.301，`rollForward: latestFeature` | — |
| 项目许可证 | **GPL-3.0** | 仓库根 `LICENSE` | 与 AtomUI LGPL-3.0 静态链接（NativeAOT）合规；发布需附 AtomUI 版权与许可证声明 |

**AtomUI 实测初始化方式**（已编译运行验证）：
```csharp
// Program.cs
AppBuilder.Configure<App>()
    .UsePlatformDetect()
    .WithAtomUIDefaultOptions()
    .WithInterFont()
    .LogToTrace();

// App.axaml.cs（OnFrameworkInitializationCompleted）
this.UseAtomUI(builder => builder.UseDesktopControls());
```

**环境与网络要求**：
- Windows NativeAOT 前置：VS Build Tools（Desktop development with C++，提供 link.exe）；Linux 需 `clang`+`zlib1g-dev`；macOS 需 Xcode CLT。
- 仓库根放置 `nuget.config`（华为云镜像优先 + nuget.org 回退）。
- Avalonia 12 API 注意：`TextBox.Watermark` 已废弃 → 使用 `PlaceholderText`。

**备选方案**（仅在风险触发时启用）：
- AtomUI + Avalonia 12 已验证可用；若未来版本升级引入问题 → 锁定当前版本并回归。
- NativeAOT 与某依赖冲突 → trimmed self-contained 发布（仍满足"免装运行时"），Core 保持 AOT 兼容。
- Sodium.Core 阻塞 → LibSodium.Net（AOT 优先，alpha）或 PBKDF2 回退。

---

## 2. 解决方案结构

源码目录与文件名全部使用 ASCII；文档可保留中文。

```
AegisVault/
├─ AegisVault.sln
├─ global.json
├─ nuget.config                          # 华为云镜像 + nuget.org 回退
├─ LICENSE                               # GPL-3.0
├─ Directory.Build.props                 # 公共属性：LangVersion、Nullable、AOT 兼容开关
├─ Directory.Packages.props              # 集中包版本（含 SQLitePCLRaw 显式锁定）
├─ .editorconfig                         # 编码 UTF-8、C# 风格
├─ .gitignore
├─ THIRD-PARTY-NOTICES.md                # 含 AtomUI (LGPL-3.0) 声明
├─ 项目规划.md / 实施方案.md / 技术可行性验证报告.md
├─ .github/workflows/ci.yml              # 构建+测试矩阵
├─ .github/workflows/release.yml         # tag 触发 AOT 打包
├─ src/
│  ├─ AegisVault.Core/                   # net10.0 类库，无 UI 依赖，IsAotCompatible=true
│  │  ├─ Models/
│  │  │  ├─ PasswordEntry.cs             # Title/Username/Password/Url/Notes/TotpSecret/Tags/CustomFields/CreatedAt/UpdatedAt
│  │  │  ├─ VaultHeader.cs               # format_version, kdf 参数, wrapped_dek
│  │  │  ├─ KdfParameters.cs
│  │  │  └─ UserConfig.cs                # 应用敏感配置（自动锁定、剪贴板、生成器默认值…）
│  │  ├─ Crypto/
│  │  │  ├─ SecureBuffer.cs              # 自研 sodium_malloc 封装（Alloc/ReadOnly/Dispose=MemZero）
│  │  │  ├─ Libsodium.Native.cs          # LibraryImport: malloc/free/memzero/mlock/munlock
│  │  │  ├─ IKeyDerivation.cs
│  │  │  ├─ Argon2idKeyDerivation.cs     # 默认：m=64MiB, t=3, p=1（参数入头部，可调）
│  │  │  ├─ Pbkdf2KeyDerivation.cs       # 回退：600k 迭代 SHA-256
│  │  │  ├─ AesGcmCipher.cs              # Encrypt/Decrypt + AAD，随机 12B nonce
│  │  │  └─ KeyEnvelope.cs               # KEK 包裹/解包 DEK
│  │  ├─ Storage/
│  │  │  ├─ VaultDatabase.cs             # SQLite 连接、WAL、事务、备份(.bak)
│  │  │  ├─ SchemaMigrations.cs          # 版本化迁移
│  │  │  └─ EntryRepository.cs           # 条目加密读写
│  │  └─ Services/
│  │     ├─ VaultService.cs              # 建库/解锁/锁定/改主密码/条目 CRUD
│  │     ├─ SecureConfigService.cs       # 懒加载单例；settings 表密文 ↔ UserConfig
│  │     ├─ PasswordGenerator.cs         # RandomNumberGenerator，长度/字符集/熵评估
│  │     ├─ TotpService.cs               # RFC 6238（HMAC-SHA1/256/512）
│  │     ├─ AutoLockPolicy.cs            # 空闲/最小化/锁屏/挂起 策略计算（纯逻辑）
│  │     └─ ClipboardPolicy.cs           # 剪贴板清除策略（纯逻辑）
│  ├─ AegisVault.Platform/               # net10.0，运行时判断 OS
│  │  ├─ IKeyProtector.cs / DpapiKeyProtector.cs
│  │  ├─ KeychainKeyProtector.cs         # macOS（M8 完整实现，先占位）
│  │  ├─ LibsecretKeyProtector.cs        # Linux（M8 完整实现，先占位）
│  │  ├─ CoreDumpGuard.cs                # setrlimit / SetErrorMode
│  │  ├─ WindowsSecureInput.cs           # CredUIPromptForWindowsCredentialsW(CREDUIWIN_SECURE_PROMPT)
│  │  └─ SessionLockWatcher.cs           # 锁屏/会话切换监听
│  └─ AegisVault.App/                    # net10.0，WinExe
│     ├─ Program.cs                      # BuildAvaloniaApp().WithAtomUIDefaultOptions()...
│     ├─ App.axaml / App.axaml.cs        # this.UseAtomUI(...)、主题、组合根
│     ├─ app.manifest                    # DPI、Windows 版本声明
│     ├─ Assets/                         # aegis.ico（含 16x16 32bpp ARGB 帧）、托盘图标、logo
│     ├─ Composition/AppServices.cs      # 手动组合根（不用反射 DI）
│     ├─ Views/
│     │  ├─ Unlock/UnlockWindow.axaml    # 首次建库 / 解锁（含安全输入开关）
│     │  ├─ Main/MainWindow.axaml        # 搜索 + 列表 + 详情三栏
│     │  ├─ EntryEditor/EntryEditorWindow.axaml
│     │  ├─ Settings/SettingsWindow.axaml
│     │  └─ FloatingBall/FloatingBallWindow.axaml  # 无边框置顶悬浮球
│     ├─ ViewModels/                     # CommunityToolkit 源生成，对应各 View
│     ├─ Controls/                       # SecurePasswordBox 等自定义控件
│     ├─ Converters/
│     └─ Services/
│        ├─ TrayService.cs               # TrayIcon 生命周期（图标按平台规格）
│        ├─ HotKeyService.cs             # 全局热键注册
│        ├─ ClipboardService.cs          # 复制 + 定时清除（内容未变才清）
│        ├─ SingleInstance.cs            # 互斥 + 唤起已有实例
│        └─ WindowStateService.cs        # 位置/大小持久化（明文 settings.json）
└─ tests/
   └─ AegisVault.Core.Tests/             # xUnit
```

---

## 3. 安全与数据设计

### 3.1 密钥体系（分层）
```
主密码 ──Argon2id(salt, m=64MiB, t=3, p=1)──> KEK(32B)
KEK ──AES-256-GCM(AAD=头部参数)──> wrapped_dek
DEK(32B 随机) ──AES-256-GCM(每条目随机 nonce)──> 条目密文
```
- 主密码绝不直接作密钥；KEK 仅用于包裹 DEK。
- 改主密码 = 新 salt 派生新 KEK → 重包 DEK → 原子更新头部，不触碰条目数据。
- 锁定时对 DEK/KEK 所在 `SecureBuffer` 执行 `sodium_memzero` 并释放。

### 3.2 解锁流程
1. 打开 vault → 读 `vault_meta` → 按 `kdf_algorithm` 派生 KEK。
2. 解包 DEK：GCM 校验失败 → 统一提示"主密码错误"（不区分错误类型）。
3. DEK 写入 `SecureBuffer` 并 `ProtectReadOnly()`；KEK 立即销毁。
4. 全量解密条目至内存模型；`VaultSession` 暴露只读视图。

### 3.3 Vault 文件格式（`.aegis` = 加密 SQLite）
```sql
CREATE TABLE vault_meta (
  id             INTEGER PRIMARY KEY CHECK (id = 1),
  format_version INTEGER NOT NULL,
  kdf_algorithm  TEXT    NOT NULL,          -- 'argon2id' | 'pbkdf2-sha256'
  kdf_salt       BLOB    NOT NULL,          -- 16B
  kdf_params     TEXT    NOT NULL,          -- JSON: {m,t,p} 或 {iterations}
  wrapped_dek    BLOB    NOT NULL,          -- GCM: nonce||ct||tag（AAD=format_version+kdf 参数）
  created_at     TEXT    NOT NULL,
  updated_at     TEXT    NOT NULL
);

CREATE TABLE entries (
  id         TEXT PRIMARY KEY,              -- GUID
  nonce      BLOB NOT NULL,                 -- 12B 随机
  ciphertext BLOB NOT NULL,                 -- PasswordEntry JSON 的 AES-GCM 密文
  tag        BLOB NOT NULL,                 -- 16B
  version    INTEGER NOT NULL,              -- 条目格式版本，参与 AAD
  updated_at TEXT NOT NULL
);

CREATE TABLE settings (
  key        TEXT PRIMARY KEY,
  nonce      BLOB NOT NULL,
  ciphertext BLOB NOT NULL,
  tag        BLOB NOT NULL
);
```
- 条目 AAD = `id + version`，防止密文换位与重放。
- **不存明文标题/URL**：搜索在解锁后的内存模型中进行（密码库量级无压力，保住零知识）。
- 写入：`BEGIN IMMEDIATE` 事务 + WAL；关闭库/保存前生成 `.bak` 滚动备份。

### 3.4 配置加密
- `SecureConfigService` 按规划要求**延迟初始化**：首次访问才解密并缓存 `UserConfig`；保存时随机 IV + GCM 重新加密写回 `settings` 表。
- 解锁后才需要的配置全部密文存储；解锁前需要的最小偏好（主题、语言、窗口位置、最近库路径）存明文 `settings.json`，不含秘密。

### 3.5 运行时内存安全
- 所有密钥/密码缓冲走自研 `SecureBuffer`：`LibraryImport` 直调 libsodium `sodium_malloc/sodium_mlock/sodium_memzero/sodium_munlock/sodium_free`（Sodium.Core 不提供该封装，实测确认；该方式无 IL2026/IL3050，NativeAOT 友好）。
- 明文条目字符串在锁定时尽力清零（`CryptographicOperations.ZeroMemory` 作用于可写缓冲；托管 string 无法控制，作为已知局限记录）。
- Avalonia `PasswordBox` 只提供 `string` → 立即转入 `SecureBuffer` 并清空绑定；后续可做自定义 `SecurePasswordBox` 改善。

### 3.6 进程硬化
- 禁用核心转储：Linux/macOS `setrlimit(RLIMIT_CORE, 0)`；Windows `SetErrorMode` 组合标志。
- 启动时不做解密/解密 IO（满足规划"延迟初始化"）。
- 日志永不输出路径、算法细节、盐、异常堆栈；统一错误码 + 用户友好文案。

### 3.7 平台保护（M6，默认关闭"记住设备"）
- `IKeyProtector.Wrap(DEK) / Unwrap()`：
  - Windows：DPAPI `ProtectedData`（CurrentUser，实测通过）。
  - macOS：Keychain（`SecItemAdd/SecItemCopyMatching`）。
  - Linux：libsecret Secret Service。
- Windows 安全输入：`CredUIPromptForWindowsCredentialsW` + `CREDUIWIN_SECURE_PROMPT`（系统安全桌面），失败回退应用内输入框。实验性开关，默认关。

### 3.8 自动化锁定
- 触发条件（任一满足）：空闲超时（默认 5 分钟，可配）、窗口最小化到托盘、系统锁屏/会话切换、系统挂起。
- 锁定即清 DEK、关闭所有敏感窗口、清剪贴板密文。

---

## 4. 功能范围

### MVP（标准版，M0–M7）
- 建库 / 解锁 / 锁定 / 改主密码；多 Vault 文件打开（单实例内切换）。
- 条目 CRUD：标题、用户名、密码、URL、备注、TOTP 密钥、标签、自定义字段。
- 实时搜索（标题/用户名/URL/标签）。
- 密码生成器：长度、大小写/数字/符号、易混淆字符排除、熵强度显示。
- TOTP 展示 + 倒计时 + 一键复制。
- 系统托盘：打开/锁定/退出；最小化到托盘；托盘失败自动降级。
- 无边框悬浮球：置顶、拖拽、点击唤起快速搜索。
- 全局热键唤起（可配置，默认 `Ctrl+Alt+A`）。
- 剪贴板复制 + 自动清除（默认 30s，内容未变才清）。
- 自动锁定（空闲/最小化/锁屏/挂起）。
- 深浅色主题（Token 化）、中英文。
- 加密导出（复制 vault 文件）/ 导入（同格式）。
- 品牌色：#1677FF 主色、#52C41A 安全色（本质为 Ant Design 默认 Token，直接采用）。

### Backlog（M8+）
- CSV / Bitwarden / Chrome / 1Password 导入。
- 密码历史、附件、自动填充（SendInput）、浏览器扩展。
- macOS Keychain / Linux libsecret 完整实现。
- TOTP 悬浮倒计时、多 Vault 并行、WebDAV/自建同步。
- 密码强度审计（弱密码/重复使用报告）。

---

## 5. 分阶段计划与验收

> 工期为单人估计（熟悉 Avalonia 前提下），MVP 合计约 12–17 个工作日。
> 本机已验证：Avalonia 12.1.2 + AtomUI 6.1.9 编译/运行、全量裁剪 0 警告且运行正常、托盘、Argon2id、AES-GCM、SQLite 3.53.3、DPAPI、sodium_malloc P/Invoke。

### M0 工程基线（0.5–1d）
- 前置：VS Build Tools 已安装并验证（MSVC 14.44 + Windows SDK 10.0.26100；POC 已完成 AOT 端到端冒烟）。
- `git init`；`LICENSE`(GPL-3.0) / `THIRD-PARTY-NOTICES.md` / `nuget.config` / `.gitignore` / `.editorconfig` / `global.json` / CPM。
- 创建 4 个工程与解决方案；锁定依赖版本（含 `SQLitePCLRaw.bundle_e_sqlite3 2.1.12` 显式引用）；`dotnet build` 通过。
- Avalonia + AtomUI 空窗口跑通（按 §1 实测初始化 API）。
- **AOT 冒烟门禁**：`dotnet publish src/AegisVault.App -c Release -r win-x64 -p:PublishAot=true`，产物可启动、无 AOT 警告。
- 验收：`dotnet test` 绿；窗口可见；AOT 单文件产物可运行。**失败即启用备选方案（见 §1）**。

### M1 密码学内核（2–3d）
- `SecureBuffer`、`Libsodium.Native` P/Invoke、`Argon2idKeyDerivation`、`Pbkdf2KeyDerivation`、`AesGcmCipher`、`KeyEnvelope`。
- 验收：RFC 9106（Argon2id）、RFC 6070（PBKDF2）、NIST GCM 测试向量全部通过；篡改 1 bit 即解密失败；错误密钥拒绝；Dispose 后内存已清零。

### M2 Vault 存储（1.5–2d）
- Schema 迁移、建库/打开/关闭、条目加密 CRUD、改主密码、`.bak` 备份、损坏文件容错。
- 验收：建库 → 增删改 → 关闭 → 重启 → 解锁，数据一致；错误密码/篡改文件均拒绝且不崩溃；改主密码后旧密码失效、数据可读。

### M3 核心服务（1d）
- `SecureConfigService`（懒加载）、`PasswordGenerator`、`TotpService`、`AutoLockPolicy`、`ClipboardPolicy`。
- 验收：RFC 6238 测试向量通过；生成器字符集/长度/排除规则单测；配置加解密往返一致。

### M4 UI 骨架（3–4d）
- UnlockWindow（建库向导 + 解锁）、MainWindow（搜索/列表/详情）、EntryEditor、Settings、主题 Token。
- 验收：手动 E2E 全流程；自动化验证 CompiledBinding 无反射；错误提示不含路径/技术栈信息。

### M5 桌面集成（2–3d）
- TrayIcon（各平台图标规格，Windows 16x16 32bpp ARGB 帧；本机已验证 TrayIcon+菜单可运行）、悬浮球、全局热键、单实例、剪贴板清除、自动锁定接线。
- 验收：三端手动清单；锁定后 DEK 已清零（单测断言）；剪贴板按策略清除；第二实例唤起主窗口。

### M6 安全硬化（2d）
- `CoreDumpGuard`、`IKeyProtector`（DPAPI 先行）、Windows CredUI 安全输入（实验开关）、锁屏/挂起监听。
- 验收：非 Windows 平台调用安全降级；开关默认关闭；锁屏事件触发立即锁定。

### M7 发布工程（1.5–2d）
- GitHub Actions：`ci.yml`（push/PR：构建 + 测试矩阵）、`release.yml`（tag：AOT 打包 win-x64/arm64、linux-x64/arm64、osx-x64/arm64）。
- NativeAOT 定稿：静态链接 libsodium（`libsodium.nativeaot`）、裁剪警告清零、单文件产物；确认 AtomUI 链接注册侧车在 CI 正常生成。
- 验收：打 tag 后从 Actions 下载产物，在**干净机器**直接运行；CI 全绿。

### M8 Backlog
- 按 §4 优先级逐项排期，每项独立验收。

---

## 6. CI/CD 要点

- CI 矩阵：`windows-latest` / `ubuntu-22.04` / `macos-14`；.NET 10 SDK。
- AOT 依赖：Windows 需 MSVC（runner 自带）；Linux 需 `clang` + `zlib1g-dev`；macOS 需 Xcode CLT。
- 发布产物命名：`AegisVault-{rid}-{version}.zip|tar.gz`，附 SHA-256。
- 随发布附 `LICENSE`(GPL-3.0) 与 `THIRD-PARTY-NOTICES.md`（含 AtomUI LGPL-3.0 声明）。
- 代码签名/公证暂不在范围内（M8+ 可选）。
- 版本号来源：Git tag（`v1.2.3`）+ `Directory.Build.props` 注入。

---

## 7. 风险与对策

| # | 风险 | 概率/影响 | 对策 |
|---|---|---|---|
| 1 | Windows NativeAOT 端到端 | 已消除 | 本机 POC 实测通过（2.74MB 控制台 / 38.74MB Avalonia+AtomUI 单文件）；CI runner 自带同等工具链 |
| 2 | AtomUI 6.1.9 + Avalonia 12.1 兼容性 | 已消除 | 实测编译/运行/裁剪通过；版本锁定，升级需回归 |
| 3 | libsodium 原生库跨平台 + AOT 打包 | 中/中 | Sodium.Core + `libsodium.nativeaot` 静态链接；KDF 字段化支持 PBKDF2 回退 |
| 4 | Linux 托盘碎片化（GNOME 需扩展、KDE 历史 NRE） | 高/低 | 托盘失败降级为悬浮球/热键；README 说明 StatusNotifierItem 要求 |
| 5 | Avalonia PasswordBox 的 string 无法清零 | 确定/中 | 立即转 `SecureBuffer`；记录已知局限；后续自定义控件与 CredUI 缓解 |
| 6 | 用户遗忘主密码（零知识无恢复） | 中/高 | 建库时强制备份提示 + 加密导出引导；文档明示 |
| 7 | AtomUI LGPL-3.0 与 NativeAOT 静态链接合规 | 已消除（决策：GPL-3.0 开源） | 项目以 GPL-3.0 开源；发布附第三方声明；不修改 AtomUI 源码 |
| 8 | AtomUI 主题系统动态换肤的 AOT 反射 | 低/中 | 链接注册机制实测有效；只用文档化 API；M0 验证换肤 |
| 9 | `api.nuget.org` 下载不稳定 | 高/低 | 仓库 `nuget.config` 华为云镜像 + nuget.org 回退 |
| 10 | SQLitePCLRaw 传递依赖回退到含漏洞版本 | 中/高 | `Directory.Packages.props` 显式固定 2.1.12；CI 检查 NU1903 |
| 11 | 中文路径在 CI/工具链 | 低/低 | 源码全 ASCII；中文仅限文档 |

---

## 8. 已确认与待确认事项

已确认：
- UI：Avalonia 12.1.2 + AtomUI 6.1.9；MVP 标准版范围。
- 许可证：项目 GPL-3.0 开源（与 AtomUI LGPL-3.0 静态链接兼容）。
- 发布路线：保留 NativeAOT；本机安装 VS Build Tools 完成 M0 门禁验证。

待确认（不影响 M0 启动）：
- "记住设备"（DPAPI/Keychain/libsecret 免密解锁）是否进 MVP：当前定为 M6 可选、默认关闭。
- 是否引入自动更新机制（后续版本）。
- 安全输入（Windows CredUI）是否对普通用户默认开启：当前定为实验开关。
