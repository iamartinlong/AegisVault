# AegisVault 代码审计报告 — M6+ / M7

> 审计对象：M6+（Windows CredUI 安全输入）与 M7（CI/Release 工作流），工作区相对 `62eeabe` 的未提交改动
> 审计日期：2026-09-13
> 结论：**未发现 Critical 问题**；发现 2 项中等发布质量缺陷与 1 项 CI 可移植性风险，建议发布前修复。

---

## 一、自动化检查

| 检查项 | 结果 |
|---|---|
| `dotnet build -c Release -warnaserror` | ✅ 0 警告 0 错误 |
| `dotnet test -c Release` | ✅ 146/146（109 Core + 4 Platform + 33 UI） |
| 依赖漏洞扫描（含新增 SystemEvents） | ✅ 6 个项目均无已知漏洞 |
| NativeAOT | ✅ 29.59MB 原生程序可运行（CredUI P/Invoke 编译通过） |
| 敏感信息/调试输出 | ✅ 未发现 |

---

## 二、发现清单

| ID | 级别 | 位置 | 问题 | 建议 |
|---|---|---|---|---|
| M67-1 | 中 | `release.yml:63-76` | 打包目录包含约 **249MB PDB**（App 148MB + Skia 80MB + HarfBuzz 20MB），发布归档将膨胀 4–5 倍 | 打包前删除 `*.pdb`，或发布加 `-p:DebugType=none -p:DebugSymbols=false` |
| M67-2 | 中 | `release.yml:76,94` | 各 RID 产物均生成同名 `SHA256SUMS`，`download-artifact merge-multiple` 会互相覆盖，最终仅剩一份、校验信息不完整 | 改为 `SHA256SUMS-${rid}` 或按 RID 分目录上传 |
| M67-3 | 中（风险） | `.github/workflows/*.yml` | App.Tests 使用真实 `App`（含 `TrayIcon`、AtomUI 主题初始化）在 headless 下运行；本地仅 Windows 验证，Linux/macOS runner 及 AtomUI Roslyn 构建任务在非 Windows 未经验证 | 首次推送后观察；若失败按平台门控 UI 测试或延迟托盘初始化 |
| M67-4 | 低 | `WindowsSecureInput.cs:42` | `ParentWindow = IntPtr.Zero`：凭据对话框无属主窗口，可能出现层级/模态体验问题 | 传入主窗口 HWND（`Window.Handle`） |
| M67-5 | 低 | `UnlockWindow.axaml.cs`（安全输入处理器） | 处理器静默吞掉所有异常；且 CredUI 提示路径无自动化测试（仅可用性断言） | 记录手动验收清单；异常时给出提示或日志（无敏感信息） |
| M67-6 | 低 | `ci.yml` | 缺少依赖漏洞扫描门禁与 NuGet 缓存 | 增加 `dotnet list package --vulnerable` 步骤；`setup-dotnet` 启用 `cache: true` |
| M67-7 | 低 | `release.yml:27-29` | `macos-13`（Intel）可能已退役，osx-x64 产物可能缺失；已用 `experimental` 容错但会静默无产物 | 发布清单中人工确认 osx-x64 产物；必要时改用 macos-14 + 交叉/替代方案 |
| M67-8 | 信息 | `release.yml:70` | `workflow_dispatch` 手动触发时 `github.ref_name` 为分支名，产物命名不一致 | 仅以 tag 触发正式发布；手动触发仅作演练 |
| M67-9 | 信息 | `WindowsSecureInput.cs:114` | 系统对话框返回的密码为托管 string（无法清零，已知局限）；缓冲已清零、解锁成功后 VM 立即清空 | 维持现状并记录于安全说明 |
| M67-10 | 信息 | `release.yml` | 无代码签名/公证；Windows SmartScreen/macOS Gatekeeper 会提示 | M8+ 计划项 |

---

## 三、复核通过项

### M6+ CredUI
- P/Invoke 正确性：`CREDUI_INFOW` 结构尺寸/字段顺序、`CREDUIWIN_GENERIC | CREDUIWIN_SECURE_PROMPT` 标志、输出缓冲 `CoTaskMemFree` 释放方式均符合文档。
- 缓冲安全：用户名/密码 UTF-16 缓冲在 `finally` 中 `ZeroMemory`；尾零裁剪逻辑正确；缓冲区不足时按失败返回 `null`（不会截断密码）。
- 取消路径：`ERROR_CANCELLED(1223)` 与其它错误统一返回 `null`。
- 平台守卫：`IsAvailable` 可跨平台调用，`PromptForPassword` 带 `[SupportedOSPlatform]`；调用方有 `OperatingSystem.IsWindows()` 守卫，CA1416 通过。
- AOT：`LibraryImport` 源生成 P/Invoke 编译通过，原生程序运行正常。

### M7 工作流
- CI 三端矩阵 + `-warnaserror` + Release 构建后测试（`--no-build`）流程正确；CI 内覆盖 `nuget.config` 为官方源，规避镜像供应链面。
- Release 触发条件（`v*` tag）、`contents: write` 权限、`softprops/action-gh-release@v2` 用法正确；Linux AOT 前置依赖（clang/zlib1g-dev）已安装。
- 归档包含 `LICENSE` 与 `THIRD-PARTY-NOTICES.md`；Windows 使用 7z、类 Unix 使用 tar.gz，路径展开（`"$PUBLISH_DIR"/*`）正确。
- 手动触发演练与新版本发布路径分离（release job 仅 tag 生效）。

---

## 四、建议

1. 优先修复 **M67-1 / M67-2**（发布产物正确性与体积），随首次推送前一并处理。
2. **M67-3** 属未知风险，首次推送 Actions 后若 Linux/macOS 失败，按审计建议门控；不阻塞 Windows 发布。
3. M67-4 / M67-6 为体验与流程增强，可在 M7 收尾时合入。
4. 修复后运行 `dotnet test`（预期 146 全绿）与 `-warnaserror` 构建，再提交。

---

## 五、修复记录（2026-09-13，已实施）

| ID | 修复内容 | 验证 |
|---|---|---|
| M67-1 | Release 打包前删除 `*.pdb`（约 249MB 调试符号），归档体积恢复至常规水平 | 工作流逻辑 + 本地 publish 目录确认 |
| M67-2 | 校验清单改为 `SHA256SUMS-<rid>`，`merge-multiple` 下不再互相覆盖 | 工作流逻辑 |
| M67-3 | 托盘改为仅在桌面生命周期内以代码创建（`App.axaml` 移除 `TrayIcon`），headless 测试不再触达托盘与原生集成 | `dotnet test` 146/146 通过 |
| M67-4 | CredUI 传入父窗口句柄（`TryGetPlatformHandle()`） | 构建 0 警告；AOT 编译验证 |
| M67-5 | 安全输入失败时提示"安全输入不可用，请手动输入主密码" | 构建 0 警告 |
| M67-6 | CI/Release 启用 NuGet 缓存；CI 增加漏洞扫描信息步骤（审计警告由 `-warnaserror` 门禁） | YAML 已更新 |
| 附加 | `THIRD-PARTY-NOTICES.md` 补齐全部许可证全文（LGPL-3.0 / Apache-2.0 / MIT / ISC），组件-许可证映射表 | 56KB，章节校验通过 |

验证结果：`dotnet build -c Release -warnaserror` 0 警告 0 错误；`dotnet test` 146/146。

