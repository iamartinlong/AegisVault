# AegisVault（盾匣）跨平台密码管理器完整项目规划

## 一、 项目概述与愿景
AegisVault（盾匣）是一款基于前沿 .NET 10 运行时与 Avalonia UI 框架构建的新一代跨平台桌面密码管理器。项目旨在通过结合 AtomUI 企业级组件库，为用户提供媲美原生应用的高性能体验与金融级的数据保护。核心愿景是贯彻“零知识（Zero-Knowledge）”与“纵深防御”的安全哲学，确保用户的数字资产在 Windows、macOS 和 Linux 平台上得到绝对的安全与隐私保护。

## 二、 品牌视觉与体验规范
* **品牌名称**：AegisVault（盾匣）
* **主题色**：深空靛蓝（#1677FF）作为主色调，配合翡翠绿（#52C41A）作为安全状态提示色。
* **UI 框架**：Avalonia UI + AtomUI。利用 AtomUI 的 Token 系统实现深色/浅色模式的无缝切换，确保跨平台视觉高度一致。
* **交互特性**：支持系统托盘（TrayIcon）后台驻留与快速唤起，以及无边框悬浮球（Floating Window）用于快捷搜索或 TOTP 倒计时展示。

## 三、 核心技术栈与架构设计
* **底层运行时**：.NET 10。利用其 NativeAOT 技术进行发布，将应用编译为独立可执行文件，无需用户安装运行时，显著降低内存占用并提升启动速度。
* **架构模式**：MVVM (Model-View-ViewModel)。严格分离 UI 视图与业务逻辑，提升代码可维护性。
* **项目目录划分**：
  * **Views**：使用 `.axaml` 文件结合 AtomUI 组件构建现代化界面。
  * **ViewModels**：使用 `CommunityToolkit.Mvvm` 处理 UI 交互逻辑与状态管理。
  * **Services**：封装核心业务逻辑（如 `CryptoService`、`SecureMemoryService`、`DatabaseService`）。
  * **Models**：定义纯数据结构（如 `PasswordEntry`、`UserConfig`）。

## 四、 核心安全架构设计（重点）
安全是盾匣的生命线，方案采用严格的纵深防御体系：

### 1. 密码学算法与数据加密
* **密钥派生 (KDF)**：严禁直接使用用户主密码作为密钥。采用 `Argon2id` 或 `PBKDF2-HMAC-SHA256` 算法，配合随机 Salt 和高迭代次数，将主密码派生为高熵的密钥加密密钥 (KEK)。
* **数据加密**：采用 `AES-256-GCM` 认证加密算法，确保数据的机密性与完整性（防篡改）。
* **分层密钥体系**：KEK 仅用于加密数据加密密钥 (DEK)，DEK 负责实际加密密码条目。更换主密码时只需重新加密 DEK，无需全量重加密。

### 2. 运行时内存安全
* **安全内存分配**：使用 `libsodium` 的 `sodium_malloc` 分配内存存放主密码与 DEK，防止敏感数据被交换到硬盘 Swap 分区。
* **及时清零**：密码使用完毕后，立即通过 `SecureZeroMemory` 等机制覆写并清零内存，绝不依赖 GC 垃圾回收。
* **防内存转储**：在应用启动时禁用进程的核心转储（Core Dump）功能，防止程序崩溃时密钥泄露。

### 3. 本地安全存储与防键盘记录
* **操作系统级保护**：若需持久化派生密钥，利用 Windows DPAPI、macOS Keychain 或 Linux libsecret 等原生安全 API，将密钥与系统登录态绑定。
* **安全桌面输入**：在 Windows 环境下，调用 `CreateDesktop` API 创建独立的安全桌面用于主密码输入，有效阻断运行在主桌面的键盘记录木马。

## 五、 关键功能模块规划
### 1. 跨平台 UI 与系统集成
* 引入 `AtomUI` NuGet 包，通过 Avalonia 的响应式布局，确保在桌面端与移动端均有一致的交互体验。
* 实现托盘图标（TrayIcon）与悬浮球（Floating Window），需严格遵循 Avalonia v11.0+ 的初始化规范与图标尺寸要求（如 Windows 下需包含 16x16 像素的 32-bit ARGB 帧）。

### 2. 配置文件与本地存储加密
* 借助 .NET 原生的 `System.Security.Cryptography` 命名空间。
* 将配置对象序列化为 JSON 后，生成随机 IV，调用 `AesGcm.Encrypt()` 进行加密，并将 IV、Tag 与密文 Base64 编码后存入本地 SQLite 数据库。

### 3. 自动化构建与发布部署
* 配合 GitHub Actions 实现 Windows、macOS、Linux 三端的自动化打包与分发。
* 结合 .NET 10 的 NativeAOT 技术进行编译，确保最终交付物的高性能与轻量化。

## 六、 开发规范与安全底线
* **禁止硬编码**：绝不可将密钥、盐值硬编码在代码或资源文件中，应通过用户交互派生或系统凭据库读取。
* **禁用弱算法**：严禁使用 AES-ECB 模式，每次加密必须生成全新的随机 IV。
* **延迟初始化**：避免在 `App.axaml.cs` 启动早期进行解密操作，推荐定义 `SecureConfigService` 单例，在首次请求时触发解密并缓存于内存。
* **异常处理精细化**：避免向用户或日志暴露底层技术栈、系统路径等敏感信息，防止攻击者利用错误提示进行进一步渗透。

