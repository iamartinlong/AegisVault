# AegisVault 技术可行性验证报告

> 方法：不依赖文档推断，直接在 Windows 本机（.NET 10.0.301 / win-x64）构建真实 POC 并运行验证。
> 验证日期：2026-09-12（NativeAOT 复验完成于 2026-09-13）
> 结论：**核心与发布技术栈全部实测可行**。AtomUI LGPL 合规问题已决策（项目 GPL-3.0 开源 + NativeAOT），Windows NativeAOT 工具链已安装并端到端验证通过。

---

## 一、验证环境

| 项 | 值 |
|---|---|
| OS | Windows 11 (10.0.22621) x64 |
| .NET SDK / 运行时 | 10.0.301 / 10.0.9 |
| MSBuild | 18.6.4 |
| Windows SDK | 10.0.26100（Build Tools 一并更新安装） |
| MSVC C++ 工具链 | **已安装**：VS Build Tools 2022，MSVC 14.44.35207（link.exe Hostx64\x64） |
| NuGet 全局缓存 | `D:\Users\.nuget\packages`（注意在 D 盘） |
| NuGet 网络 | api.nuget.org 大文件下载不稳定（EOF/SSL 中断）；华为镜像稳定可用 |
| Docker | 已安装但守护进程未运行（Linux/macOS 验证改由 CI 承担） |

POC 位置：`C:\Users\EDY\AppData\Local\Temp\opencode\aegis-feas\`
- `CoreProbe`：Sodium.Core + Microsoft.Data.Sqlite + DPAPI 控制台探针（含 NativeAOT）
- `UiProbe`：Avalonia 12.1.2 + AtomUI 6.1.9 桌面应用（含托盘图标，含 NativeAOT）

---

## 二、验证矩阵（全部为实测结果）

| # | 验证项 | 方法 | 结果 | 证据/备注 |
|---|---|---|---|---|
| 1 | Avalonia 12.1.2 + AtomUI 6.1.9 + net10.0 编译 | `dotnet build -c Release` | ✅ | 0 错误；仅 2 条 `TextBox.Watermark` 废弃警告 |
| 2 | AtomUI 窗口运行 | 启动 exe 8s 观察 | ✅ | 窗口正常显示；`TextBlock/LineEdit` 正常 |
| 3 | AtomUI 初始化 API | IL 反汇编 + 实际调用 | ✅ | `AppBuilder.WithAtomUIDefaultOptions()`；`Application.UseAtomUI(b => b.UseDesktopControls())` |
| 4 | Avalonia TrayIcon + 托盘菜单 | App.axaml 增加 `TrayIcon.Icons` + PNG 资源 | ✅ | 进程稳定存活；构建 0 错误 |
| 5 | libsodium 原生库加载（.NET 10） | Sodium.Core 1.4.1 | ✅ | `libsodium 1.0.22` |
| 6 | Argon2id 密钥派生 | `PasswordHash.ArgonHashBinary` (Argon2id13) | ✅ | 190–260ms/次（Interactive 档）；确定性/错密码区分正确 |
| 7 | 安全内存（sodium_malloc） | **Sodium.Core 无 SecureMemory API** → 自研 `LibraryImport` P/Invoke | ✅ | `sodium_malloc/sodium_memzero/sodium_free` 可用；原生 AOT 下同样通过 |
| 8 | AES-256-GCM | BCL `AesGcm`（32B 密钥、12B nonce、16B tag、AAD） | ✅ | 往返一致；篡改 1 bit 即 `CryptographicException` |
| 9 | SQLite 读写 | Microsoft.Data.Sqlite | ✅ | BLOB 往返一致（framework + 裁剪 + AOT 三种模式） |
| 10 | SQLite 安全版本 | 升级 `SQLitePCLRaw.bundle_e_sqlite3 2.1.12` | ✅ | **SQLite 3.53.3**，修复 CVE-2025-6965（≤2.1.11 受影响，High） |
| 11 | DPAPI | `ProtectedData 10.0.12` CurrentUser | ✅ | Protect/Unprotect 往返一致（原生 AOT 下同样通过） |
| 12 | 裁剪发布（trim, 控制台） | `PublishTrimmed=true, TrimMode=full` | ✅ | 仅 1 条 IL2026（探针自身反射代码）；裁剪 exe 运行全部通过 |
| 13 | 裁剪发布（trim, Avalonia+AtomUI） | 同上 | ✅ | **0 条 ILLink 警告**；裁剪后应用正常运行（AtomUI 裁剪注册生效） |
| 14 | AtomUI 的 AOT/裁剪配套机制 | 分析 `buildTransitive/*.targets` + IL 元数据 | ✅ | 见 §三.2 |
| 15 | **NativeAOT 发布（控制台）** | `PublishAot=true` | ✅ | 2.74MB 原生单文件；ILC 编译 + 链接 + 运行全通过 |
| 16 | **NativeAOT 发布（Avalonia+AtomUI）** | `PublishAot=true` | ✅ | **38.74MB 原生单文件**；窗口 + 托盘图标正常，ILC 零 AOT 警告 |
| 17 | NuGet 镜像 | 华为云 `v3/index.json` | ✅ | 18MB 包 36s 完成；本机已用临时 `nuget.config` |

---

## 三、关键发现

### 1. ⚠️→✅ AtomUI 是 LGPL-3.0 许可证（已决策：项目 GPL-3.0 开源）

`AtomUI.Desktop.Controls 6.1.9` 包内 LICENSE 为 **GNU LGPL v3**，README 明确：商业应用在"链接已发布二进制"前提下可免费使用；修改源码则须开源或购买商业许可。

**决策结果**：本项目以 **GPL-3.0 开源**，与 AtomUI LGPL-3.0 静态链接（NativeAOT）合规；不修改 AtomUI 源码；发布时附 `THIRD-PARTY-NOTICES.md` 声明。若未来改为闭源商业产品，需重新评估（购买商业许可或放弃 AOT）。

### 2. ✅ AtomUI 6 内置 AOT/裁剪支持（已被端到端验证）

`buildTransitive/AtomUI.LinkedRegistration.targets` 显示：
- `PublishTrimmed=true` 或 `PublishAot=true` 时自动置 `AtomUILinkedPublish=true`；
- 通过 Roslyn 构建任务生成"链接注册计划"侧车（`*.atomui-link.json`），编译期替代反射注册控件/主题；
- 编译期校验 `ATOMUILINK001`（应用注册计划标记）；
- 运行时配置 `AtomUI.AotTrimRegistration.Enabled=true`（仅 AOT 主编译时注入）。

实测：全量裁剪与 NativeAOT 均 **0 条警告**，原生单文件应用正常运行。

### 3. ✅ 初始化 API 与版本配对确认

- 实测可用的初始化方式：
  ```csharp
  // Program.cs
  AppBuilder.Configure<App>()
      .UsePlatformDetect()
      .WithAtomUIDefaultOptions()   // AtomUI 扩展
      .WithInterFont()
      .LogToTrace();

  // App.axaml.cs
  this.UseAtomUI(builder => builder.UseDesktopControls());
  ```
- AtomUI 6.1.9 依赖 Avalonia **12.1.x**（实测 12.1.2 正常）。
- XAML 命名空间：`xmlns:atomui="https://atomui.net"`。
- AtomUI 控件（`LineEdit`、`TextBox` 等）位于 `AtomUI.Desktop.Controls`，部分控件以 Token 形式改造 Avalonia 原生控件。

### 4. ⚠️ SQLite 必须锁定安全版本

- 默认传递依赖为 `SQLitePCLRaw.bundle_e_sqlite3 2.1.11`（SQLite 3.49.1，**含 CVE-2025-6965**，High，EPSS 75%）。
- 显式引用 **`SQLitePCLRaw.bundle_e_sqlite3 2.1.12`**（或 Microsoft.Data.Sqlite 10.0.12+）后为 SQLite 3.53.3，漏洞消除。
- 实施时必须显式锁定，防止还原回退到 2.1.11。

### 5. ✅ Sodium.Core 能力边界：没有 SecureMemory

- Sodium.Core 1.4.1 只提供算法 API（含 Argon2id13），**不提供 `sodium_malloc` 封装**（IL 中无相关类型）。
- 实测自研 `LibraryImport` P/Invoke 直接调用 `sodium_malloc / sodium_memzero / sodium_free` 完全可行且满足 NativeAOT（无 IL2026/IL3050 警告），原生单文件运行通过。
- 备选：`LibSodium.Net`（0.16.0-alpha，AOT 优先）或 `libsodium.nativeaot`（静态库，AOT 链接用）。

### 6. ✅ Windows NativeAOT 工具链（已安装并验证）

- 过程记录：初次 `PublishAot` 因缺 MSVC 报 `Platform linker not found`；安装 VS Build Tools（VCTools 工作负载）过程中首次链接报 `LNK1104: 无法打开文件 "ucrt.lib"`（安装尚未完成、UCRT 路径未注册）；安装完成后重跑，**NativeAOT 全流程通过**。
- 结论：Windows AOT 只需 VS Build Tools（含 C++ 工作负载）；GitHub Actions `windows-latest` 自带，无需额外配置。
- 注意：使用 `System.Security.Cryptography.ProtectedData` 时编译器给 **CA1416** 警告（仅 Windows 支持）——在 `AegisVault.Platform` 中用 `[SupportedOSPlatform("windows")]`/运行时判断消除。

### 7. ✅ NuGet 网络需镜像加速

- `api.nuget.org` 下载 20MB 包会长时间卡死/EOF；同时小包正常。
- 华为云镜像 `https://mirrors.huaweicloud.com/repository/nuget/v3/index.json` 稳定（18MB/36s）。
- 建议在仓库放置 `nuget.config`（镜像 + nuget.org 回退），可显著缩短首次还原时间。

### 8. 其他实测细节

- Avalonia 12 中 `TextBox.Watermark` 已废弃 → 用 `PlaceholderText`。
- `Avalonia.Headless.XUnit 12.1.2` 可用（单元测试方案保留）。
- 本机 NuGet 全局缓存位于 `D:\Users\.nuget\packages`，脚本中勿写死 C 盘路径。
- NativeAOT 产物构成（UiProbe 示例）：主 exe 38.74MB；仍需随包分发的原生库 `libSkiaSharp.dll`、`av_libglesv2.dll`、`libHarfBuzzSharp.dll`、`e_sqlite3.dll`、`libsodium.dll`（如需真正单文件/静态链接，M7 用 `libsodium.nativeaot` 与 Skia 静态链接方案评估）。

---

## 四、遗留验证项（不阻塞开工，均有明确路径）

| 项 | 说明 | 补齐方式 |
|---|---|---|
| Linux / macOS 构建与运行 | 本机为 Windows，Docker 守护进程未启动 | CI 矩阵（ubuntu/macos runner）验证 |
| Linux 托盘（StatusNotifierItem） | 需 Linux 桌面环境 | CI 冒烟 + 实机测试；GNOME 需 AppIndicator 扩展 |
| Windows 安全输入（CredUI Secure Prompt） | 需交互验收 | M6 实机测试 |
| argon2 参数调优（内存/时间） | 与机器相关 | M1 基准测试后定稿（建议 64MiB/3 轮起步） |
| macOS Keychain / Linux libsecret | 需对应系统 | M8 对应平台实现与验证 |
| 全局热键 / 悬浮球多平台观感 | 需实机 | M5 三端手动清单 |

---

## 五、结论

**技术可行性结论：全部核心项实测通过，可全速实施。**

- 已验证：.NET 10 + Avalonia 12.1.2 + AtomUI 6.1.9 编译/运行/全量裁剪/NativeAOT 单文件/托盘；libsodium Argon2id；自研安全内存（AOT 兼容）；AES-256-GCM；SQLite 3.53.3（安全版本）；DPAPI。
- 合规：项目 GPL-3.0 开源 + AtomUI LGPL-3.0 静态链接方案成立。
- 工具链：Windows NativeAOT 已在本机跑通；CI runner 自带同等能力。
- 待办：Linux/macOS 与平台特有功能（托盘、Keychain/libsecret、CredUI）由后续里程碑在 CI/实机验证。

**对《实施方案.md》的修订已执行**：版本锁定（SQLitePCLRaw 2.1.12 / ProtectedData 10.0.12）、SecureBuffer 自研 P/Invoke、AtomUI LGPL + GPL-3.0 决策、M0 AOT 门禁与工具链前置、Avalonia 12 API 细节、NuGet 镜像配置。
