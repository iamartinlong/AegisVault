# AegisVault 代码审计报告 — M5 桌面集成

> 审计对象：工作区相对 `62eeabe` 的未提交改动（M5：托盘/最小化/剪贴板/自动锁定/生成器/文件选择器）
> 审计日期：2026-09-13
> 结论：**未发现 Critical 问题；发现 4 项中等风险，需修复后再提交。**

---

## 一、自动化检查

| 检查项 | 结果 |
|---|---|
| `dotnet build -c Release -warnaserror` | ✅ 0 警告 0 错误 |
| `dotnet test -c Release` | ✅ 123/123（102 Core + 21 UI） |
| 依赖面变化 | 无新增 NuGet 包（沿用上次漏洞扫描结论） |
| 新增敏感信息/调试输出 | 无 |

---

## 二、发现清单

| ID | 级别 | 位置 | 问题 | 建议 |
|---|---|---|---|---|
| F1 | 中 | `ClipboardService.cs:60`（Dispose）、`App.axaml.cs`（CleanupSession） | **锁定/退出时不清剪贴板**：Dispose 仅停止计时器，若密文仍在剪贴板会无限期残留，违背实施方案 §3.8"锁定即清剪贴板密文" | Dispose 时尽力执行一次 `ClearIfUnchangedAsync()`；托盘退出前先清理会话 |
| F2 | 中 | `GeneratorWindow.axaml.cs:15-25` | 生成器"复制"直接写系统剪贴板，**绕过 ClipboardService 的自动清除**，生成的密码长期驻留 | 将会话 ClipboardService 注入对话框（可选属性），有服务时走自动清除 |
| F3 | 中 | `MainWindow.axaml.cs:37`、`AutoLockService.cs:44` | **托盘恢复后可能立即重锁**：`SetMinimized(false)` 不刷新 `_lastActivity`，隐藏超过空闲阈值后恢复，下一次 tick 会按空闲触发锁定 | `Activated` 改调 `ReportActivity()`（同时复位活动时间与最小化标志） |
| F4 | 中 | `UserConfig.cs:8` | `LockOnMinimize` 默认 `false`，与实施方案 §3.8（最小化到托盘为默认触发条件）不符 | 默认值改 `true`（如产品有意变更需同步修订方案） |
| F5 | 低 | `MainWindow.axaml.cs:30-34` | 最小化处理顺序为"先 `SetMinimized(true)`（可能同步触发锁定并关闭窗口）后 `Hide()`"，对已关闭窗口操作 | 调整为 `Hide()` 在前、`SetMinimized(true)` 在后 |
| F6 | 低 | `App.axaml.cs:149` | 托盘"退出"直接 Shutdown，未走 `CleanupSession`（DEK 依赖进程回收、剪贴板未清） | 退出前调用清理（与 F1 同批） |
| F7 | 低 | `MainWindow.axaml.cs:58-70`、`MainViewModel.HasTotp` | TOTP 密钥非法时按钮仍可点，会把占位文案"无效密钥"写入剪贴板 | 复制前校验 `TotpCode` 是否为纯数字（6/8 位） |
| F8 | 低 | `ClipboardService.cs:17,36` | 为比对"内容未变"在内存中保留复制明文的 string（最长一个清除周期） | 可改用 SHA-256 指纹比对，减少明文驻留 |
| F9 | 信息 | `AutoLockService` | 锁屏/挂起触发未接线、空闲评估粒度 10s（最长延迟 10s） | M6 计划内；粒度可接受 |
| F10 | 信息 | `Assets/*.png` | 图标为程序生成占位图 | 发布前替换为正式品牌图标 |

---

## 三、复核通过项

- 剪贴板清除策略正确：仅在剪贴板内容与复制值完全一致时清除（`ClipboardPolicy` 单测覆盖），不覆盖用户新内容。
- 自动锁定策略优先级正确（挂起 > 锁屏 > 最小化 > 空闲），策略单测覆盖；服务在锁定触发后自停。
- 会话生命周期：锁定路径先 `CleanupSession` 再关闭主窗口，`Closed` 处理器通过引用比对避免重复清理；事件订阅可释放。
- 新剪贴板 API（Avalonia 12 `DataTransfer` 模型）使用正确：读回强制 Dispose、写入不 Dispose（遵循平台约定）。
- 生成器选项边界处理：无字符类/长度过短时空值降级不崩溃（单测覆盖）。
- 文件选择器限制 `*.aegis`，异常静默不崩溃；AOT 原生运行验证通过（29.53MB，托盘 + 解锁窗口）。
- 测试新增覆盖：剪贴板 4 项、生成器 5 项、自动锁定 2 项。

---

## 四、建议

1. 优先修复 F1–F4（安全/功能缺陷），F5–F8 随同批处理。
2. 修复后补充测试：锁定/退出清剪贴板（F1）、托盘恢复不误锁（F3，可在 `AutoLockService` 增加可控时钟或测试 `ReportActivity` 行为）。
3. 全量回归（预期 123+ 全绿）+ `-warnaserror` 构建后提交。
