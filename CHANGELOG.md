# 变更日志

AegisVault（玄钥）每个发布版本的用户可见变更。格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

**维护约定（发版必读）**

1. 发版前在下方新增 `## [vX.Y.Z] - YYYY-MM-DD` 段落，**tag 名必须与段落名一致**（`v` 前缀 + 三段版本号）。
2. 段落内按 `新增 / 变更 / 修复 / 移除 / 安全 / 升级须知` 分组，只写**用户可见**的变更，不必罗列内部重构。
3. **涉及落盘格式（库头 / 条目负载 / 设置负载 / 明文偏好）时必须写「升级须知」**，并说明旧版本能否读新库。
4. `release.yml` 会在缺少对应段落时**直接失败**（changelog 守卫），GitHub Release 正文取自该段落——因此这里是发布说明的**唯一真源**，不要再依赖自动生成的 notes。
5. 未发布的改动先记在 `## [Unreleased]` 下。

## [Unreleased]

## [v0.3.0] - 2026-09-21

### 新增

- **树形分类（最多 4 级）**：侧栏分类改为可展开/收起的树，选中父分类即筛选整枝；行尾「…」菜单提供「新建子分类 / 编辑分类 / 移动到… / 合并到… / 删除分类」；分支行显示聚合条目数，展开箭头随状态翻转。
- **中文界面标题「玄钥」**：中文模式下窗口标题（含**任务栏 / Alt+Tab**）、自绘标题栏、启动页与首启页品牌字均显示「玄钥」；英文模式仍为 `AegisVault`。

### 修复

- **分类结构变更补齐「同父重名」不变量**：把分类移动到已有同名兄弟的父级会被拒绝并给出本地化提示（此前会静默产生两个同名分类）；合并分类撞名时整体拒绝、不留半成品树；删除父分类时子分类上移遇重名自动改名「名称 (2)」并在状态栏说明；打开旧库时**加载期自动去重**（保留最旧、其余改名，绝不丢分类）。
- 分类树缩进按 AtomUI 的 0 基层级修正（子分类缩进恢复为每级 16px）。
- 解锁页错误提示落在对应字段下方、内容区不再固定高度。

### 变更

- 设置 → 关于 的版本号与发布 tag 对齐（此前停留在 `0.1.0`）。
- README 中文截图更新（树形侧栏 + 详情面板），下载示例版本号更新为 `v0.3.0`。

### 升级须知

- **分类负载升级为 v3**：新增 `ParentId`（层级）并把版本写入 AAD（`AegisVault|settings|categories|v3`）。v1/v2 的旧库（裸列表）会在解锁后**自动升级并重写**，无需手动操作。
- **升级后请勿再用 v0.2.0 或更早版本打开同一个库**：旧版本读不到 v3 分类，会显示为「无分类」（**条目数据完好，并未丢失**）。如需回退旧版本，请先用备份，或先把库复制一份。
- 条目负载仍为 **v4**，本次**未变化**；主密码、设备免密、文件权限均不受影响。
- ⚠️ **已知打包瑕疵**：`AegisVault-win-x64-v0.3.0.zip` 解包后位于 `src/AegisVault.App/bin/Release/net10.0/win-x64/publish/` 嵌套目录内（下一版本修复）；解包后进入该目录运行 `AegisVault.App.exe`。

## [v0.2.0] - 2026-09-19

首个产出正式产物的版本：**Windows / Linux / macOS（x64 与 arm64）四平台 NativeAOT 单文件**。

### 新增

- **三栏主窗口**（分类 / 列表 / 详情）+ 自绘标题栏、内容卡、视图与排序控件。
- **设置窗口**：安全（自动锁定 / 剪贴板 / 改主密码 / 设备密钥）、外观（主题三态 / 语言一键重启）、数据（备份 / 数据目录）、关于。
- **密码强度评估**与生成器增强（强度分、破解时间、建议；生成器支持长度/符号等设置并落盘）。
- **快速访问浮层**（`Ctrl+Shift+Space`）+ 全局热键、托盘菜单、可选悬浮球（拖动/磁吸/位置记忆）。
- **分类与标签双模型**：分类 CRUD + 颜色、标签自由文本筛选、未分类视图。
- **安全健康面板**：弱密码 / 重复使用 / 超过一年未更新（含「待更新」视图与安全摘要）。
- **CSV 导入**（Bitwarden 与通用格式，自动识别表头）。
- **中英双语界面**（静态字符串表，设置内切换，重启生效）。
- **启动体验**：品牌 splash → 初始化 → 目的地窗口（消除「解锁窗闪一下再跳主窗」）；设备免密直达主窗。
- **首次启动页**：欢迎块、密码规则实时提示、路径实时提示、拖拽 `.aegis` 打开、最近库列表、一次性三步引导。
- **单实例**：第二次启动激活已有窗口而不是新开进程。
- **开机自启 / 启动最小化到托盘**（Windows，注册表为真源）。
- 条目字段扩展：手机号、**邮箱**、AppID、Secret、API Key（Secret/API Key 打码显示、不参与搜索）。
- 安全强化：防截屏（按窗口）、剪贴板历史/云同步排除、退出时清理剪贴板、库文件仅属主可读（Unix）。

### 修复

- 分类/标签筛选后列表选中项丢失；详情面板空状态与选中态。
- 搜索与健康分析卡顿（去抖 + 后台分析 + revision 守卫），大库启动与导入明显更快（SQLite `synchronous=NORMAL`）。
- 审计批次 1–3 共 46 项：会话/窗口生命周期崩溃与泄漏、导入跨线程写库、损坏负载异常穿透、改密后设备密钥失效、版本错误统一报 `UnsupportedVersion` 等。
- macOS AOT 链接失败（缺 Swift 兼容 shim stub）→ 补 SDK sysroot 搜索路径 + 兜底，**首次四平台产物齐全**。

### 升级须知

- 条目负载升级到 **v4**（新增 `Email`）；旧库解锁时自动重写升级。
- 引入版本化负载迁移框架：未知的**更高**版本负载会明确报「版本不支持」，不再被静默读成空数据。

## [v0.1.0] - 2026-09-15

首个 tag（**未产出 Release**：macOS AOT 链接尚未修复，故没有可下载产物）。

### 新增

- 核心密码库引擎：Argon2id → KEK → 包裹 DEK 的分层密钥、AES-256-GCM 加密负载、libsodium 安全内存、设备密钥（DPAPI）、备份与改密。
- 本地加密 SQLite 存储（`.aegis`）+ 版本化库头与 schema 迁移。
- Avalonia 桌面界面骨架：解锁/创建窗口、三栏主窗口雏形、设置与生成器窗口。
- 平台能力：托盘、热键、防截屏、剪贴板策略、单实例基础、核心转储防护。
- CI 与 NativeAOT 发布工作流、第三方许可证清单（GPL-3.0 / LGPL-3.0 合规）。

[v0.3.0]: https://github.com/iamartinlong/AegisVault/compare/v0.2.0...v0.3.0
[v0.2.0]: https://github.com/iamartinlong/AegisVault/compare/v0.1.0...v0.2.0
[v0.1.0]: https://github.com/iamartinlong/AegisVault/releases/tag/v0.1.0
