# AegisVault 代码审计报告

> 审计对象：本地提交 `31e5d31`（feat: initial baseline with core vault engine and UI shell (M0-M4)）
> 审计日期：2026-09-13
> 审计范围：全部已提交源码（Core / Platform / App / Tests / 构建配置）
> 结论：**未发现 Critical / Major 安全问题**；发现 1 项中等硬化缺口与若干低风险改进项。

---

## 一、自动化检查结果

| 检查项 | 命令/方法 | 结果 |
|---|---|---|
| 依赖漏洞 | `dotnet list package --vulnerable --include-transitive` | ✅ 5 个项目均无已知漏洞（`SQLitePCLRaw 2.1.12` 显式钉住生效，原 2.1.11 的 CVE-2025-6965 已规避） |
| 警告即错误构建 | `dotnet build -c Release -warnaserror` | ✅ 0 警告 0 错误 |
| 测试 | `dotnet test -c Release` | ✅ 107/107 通过（97 Core + 10 UI 无头） |
| NativeAOT | `dotnet publish -r win-x64 -p:PublishAot=true` | ✅ 单文件原生运行（29.23MB） |
| 敏感信息扫描 | 私钥/API Key/硬编码密钥正则 | ✅ 未发现 |
| 代码卫生 | TODO/FIXME/HACK、调试输出、空 catch | ✅ 未发现；宽 catch 均为回滚/清理路径（VaultService/Open 封装、VaultDatabase、Pbkdf2） |
| 仓库卫生 | `git ls-files` / status | ✅ 66 文件，无 bin/obj/数据库文件，工作区干净 |

---

## 二、发现清单

| ID | 级别 | 位置 | 问题 | 建议 |
|---|---|---|---|---|
| F1 | 中（硬化缺口） | `VaultService.cs:151`（Unlock 成功路径）、`:76`（CreateNew） | DEK 会话密钥始终可写，未按实施方案 §3.2 调用 `SecureBuffer.ProtectReadOnly()` | Unlock/CreateNew 成功后立即 `_dek.ProtectReadOnly()`；所有读取路径已使用 `ReadOnlySpan`，改动零影响 |
| F2 | 低 | `VaultService.cs:112` | `Unlock` 未校验 `_header.FormatVersion > CurrentFormatVersion`，新版本库在旧程序上可能表现为 Corrupted 而非明确拒绝 | 入口处显式校验并返回 `Corrupted`（可选新增 `UnsupportedVersion` 状态） |
| F3 | 低（UI 健壮性） | `MainViewModel.cs:198-207` | `ReplaceInList` 全量重建 `FilteredEntries`，真实 ListBox 绑定下可能丢失选中项/触发编辑器清空；无头测试未覆盖真实控件选择同步 | 保存时就地替换 `Entries` 与 `FilteredEntries` 中的实例，避免整表重建 |
| F4 | 低 | `VaultDatabase.cs:36-59` | `OpenOrCreate` 对任意文件执行建表迁移，用户误选非库文件会被写入副作用 | 先用只读连接探测 `vault_meta` 是否存在，确认是库后再以读写方式打开并迁移 |
| F5 | 低 | `EntryRepository.cs:15`、`VaultService.LoadEntries` | 未拒绝 `version > EntryFormatVersion` 的条目记录（当前仅 v1 无实际影响） | 解密前校验版本上限，超出即视为损坏 |
| F6 | 低 | `Argon2idKeyDerivation.cs:31` | 本地校验允许 `iterations >= 1`，但 Sodium.Core 强制 `>= 3`，错误信息来源不一致 | 本地下限改为 3，给出清晰参数错误 |
| F7 | 信息 | `PasswordEntry` / `UnlockViewModel` | 托管 string（主密码、条目密码）无法主动清零，属零知识内存治理的已知局限 | 规划已记录；M4+ 评估自定义安全输入控件与锁定后字段覆写 |
| F8 | 信息 | `nuget.config` | 华为云镜像映射全部包（网络环境所需），属第三方供应链面 | 本地开发可接受；建议 CI/发布流水线仅使用官方 nuget.org |
| F9 | 信息 | `Directory.Build.props` | `TreatWarningsAsErrors` 未默认开启（本地已按 `-warnaserror` 验证通过） | CI 构建加 `-warnaserror`（M7） |
| F10 | 信息 | `THIRD-PARTY-NOTICES.md` | 第三方许可证全文待补（含 AtomUI LGPL-3.0） | 发布前补齐（M7 验收项） |

---

## 三、安全设计复核（通过项）

- 分层密钥：主密码 → Argon2id（64MiB / 3 轮，盐 16B）→ KEK；KEK 仅包裹 32B 随机 DEK；AES-256-GCM 认证加密，每次加密随机 12B nonce。
- 头部 AAD 绑定 `format_version + kdf 参数 + salt`，篡改参数将导致解封失敗（统一表现为"主密码错误"，不泄露细节）。
- 条目 AAD 绑定 `id + version`，防密文换位/重放；测试覆盖逐字节篡改拒绝。
- 数据库文件不含明文条目/配置（测试断言）；`settings` 密文独立 AAD。
- 密码学实现直接依赖 libsodium（1.0.22）与 BCL `AesGcm`，无自研算法；KDF/PBKDF2 均通过官方向量测试。
- 错误面向用户统一话术，无路径/堆栈/算法细节泄露（UnlockViewModel 统一错误）。
- 测试覆盖面：KDF 确定性/参数敏感、GCM 篡改、信封错误密钥、vault 往返/错密/篡改/备份、TOTP RFC 6238 三算法向量、生成器字符集/熵。

---

## 四、结论与建议

1. **推荐优先修复 F1**（一行改动级别的硬化缺口，修复后与实施方案一致）。
2. F2–F6 属低风险改进，建议在 M4+ 内随 UI 打磨一并处理，并补充对应单测（F2/F5 可加负例测试）。
3. F7–F10 为过程性事项，已纳入 M7 发布工程验收。

修复实施后建议运行 `dotnet test`（预期 107+ 全绿）并追加一次 `-warnaserror` 构建，再行提交。

---

## 五、修复记录（2026-09-13）

| ID | 修复内容 | 验证 |
|---|---|---|
| F1 | Unlock/CreateNew 成功后对 DEK 调用 `ProtectReadOnly()` | 全部解锁/CRUD 测试通过（mprotect 失败会抛异常） |
| F2 | `Unlock` 入口校验格式版本，新增 `UnsupportedVersion` 状态与 UI 提示"请升级应用" | 新增 `NewerFormatVersionReturnsUnsupportedVersion` |
| F3 | 列表改为原地增删/替换（`MatchesFilter` 判定），不再整表重建，避免选中项丢失 | MainViewModel 既有测试全部通过 |
| F4 | 打开前只读探测 `vault_meta`；`Open` 对缺失文件直接报错不再创建文件 | 新增 `OpenNonVaultFileThrowsWithoutModifyingIt`、`OpenMissingFileThrowsWithoutCreatingIt` |
| F5 | `LoadEntries` 拒绝版本高于当前支持的条目 | 新增 `NewerEntryVersionReportsCorrupted` |
| F6 | Argon2id 迭代下限本地校验与 libsodium 对齐（>=3） | 新增 `RejectsTooFewIterations` |

验证结果：`dotnet build -c Release -warnaserror` 0 警告 0 错误；`dotnet test` 112/112 通过（102 Core + 10 UI）。

